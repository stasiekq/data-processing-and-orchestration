# Used with: dbt run --profiles-dir .
# When cwd is dbt/, DuckDB file is created under dbt/target/
ecommerce_analytics:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: target/pipeline.duckdb
      threads: 4
